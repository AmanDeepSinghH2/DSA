#include <bits/stdc++.h>
using namespace std;

vector<int> productExceptSelf(vector<int>& nums){
    int n=nums.size();
    int pr;
    vector<int> answer (n);
    for(int i=0;i<n;i++){
        
    }

}